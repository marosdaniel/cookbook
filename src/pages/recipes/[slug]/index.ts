// /reciepes/toltott-kaposzta

// /reciepes/me/toltott-kaposzta
// /reciepes/username/toltott-kaposzta

// [slug]/[slug].tsx does not work: You cannot have the same slug name "slug" repeat within a single dynamic path
const RecipeDetailPage = () => {
  return null;
};

export default RecipeDetailPage;
